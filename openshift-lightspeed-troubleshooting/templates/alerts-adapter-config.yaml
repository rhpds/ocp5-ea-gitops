apiVersion: v1
kind: ConfigMap
metadata:
  name: alerts-adapter-config
  namespace: openshift-lightspeed
data:
  config.yaml: |
    pollInterval: 30s
    initialDelay: 5m
    cooldownWindow: 1h
    filtering:
      allowedReceivers:
        - critical
