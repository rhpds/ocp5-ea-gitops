gateway:
  name: mcp-gateway
  namespace: openshift-ingress

serversNamespace: mock-mcp-servers

servers:
  - name: inventory
    hint: "Warehouse inventory management - products, stock levels, alerts"
    categories:
      - operations
  - name: accounts
    hint: "Customer account management - profiles, credit, order history"
    categories:
      - finance
