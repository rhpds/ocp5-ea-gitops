---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: agentic-skills
  namespace: {{ .Values.namespace }}
spec:
  output:
    to:
      kind: ImageStreamTag
      name: agentic-skills:latest
  source:
    type: Git
    git:
      uri: {{ .Values.git.skills.uri }}
      ref: {{ .Values.git.skills.ref }}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Containerfile
  triggers: []
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: cvo-patched
  namespace: {{ .Values.namespace }}
spec:
  output:
    to:
      kind: ImageStreamTag
      name: cvo-patched:latest
  source:
    type: Binary
  strategy:
    type: Docker
  resources:
    {{- toYaml .Values.build.resources | nindent 4 }}
  triggers: []
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: agentic-console
  namespace: {{ .Values.namespace }}
spec:
  output:
    to:
      kind: ImageStreamTag
      name: lightspeed-agentic-console:latest
  source:
    type: Git
    git:
      uri: {{ .Values.git.agenticConsole.uri }}
      ref: {{ .Values.git.agenticConsole.ref }}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  resources:
    {{- toYaml .Values.build.resources | nindent 4 }}
  triggers: []
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: cluster-update-console
  namespace: {{ .Values.namespace }}
spec:
  output:
    to:
      kind: ImageStreamTag
      name: cluster-update-console-plugin:latest
  source:
    type: Git
    git:
      uri: {{ .Values.git.clusterUpdateConsole.uri }}
      ref: {{ .Values.git.clusterUpdateConsole.ref }}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  resources:
    {{- toYaml .Values.build.resources | nindent 4 }}
  triggers: []
